import type {
  Finding,
  Observation,
  WeekInput,
} from "../types";

export async function generateObservations(
  _input: WeekInput,
  _findings: Finding[]
): Promise<Observation[]> {
  return [];
}